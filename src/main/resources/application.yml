server:
  port: 8080

spring:
  application:
    name: velas

  datasource:
    url: jdbc:postgresql://localhost:5432/velas
    username: postgres
    password: Borman15
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    database: POSTGRESQL

